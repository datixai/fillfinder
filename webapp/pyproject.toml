[project]
name = "fillfinder-web"
version = "2.0.0"
requires-python = ">=3.10"
dependencies = [
    "pyembroidery>=1.5",
    "shapely>=2.0",
    "numpy>=1.21",
]

[tool.vercel]
entrypoint = "api.index:handler"
